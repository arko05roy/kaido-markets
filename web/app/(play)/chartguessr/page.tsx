// ChartGuessr — the launch game (build.md E12). Sprint 0: placeholder route.
// The 45s build / 15s draw / reveal / auto-payout loop arrives in Sprint 3.
export default function ChartGuessrPage() {
  return (
    <main className="flex flex-1 flex-col items-center justify-center gap-2 p-8 text-center">
      <h1 className="text-2xl font-semibold">ChartGuessr</h1>
      <p className="text-muted-foreground">
        Coming in Sprint 3: watch 45s of BTC, draw a path, get auto-paid.
      </p>
    </main>
  );
}

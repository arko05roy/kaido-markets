# curve

Deterministic path/hump → Gaussian-per-checkpoint `(μ, σ)` fit. Shares
`docs/test-vectors/*.json` with `kaido-math`; a mismatch fails CI on both the
Rust and TS sides (ADR-8, build.md §6 item 5).

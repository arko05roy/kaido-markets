//! Integration-test crate for Kaido. Test files live in `tests/`; this lib is
//! intentionally empty (it only exists to anchor the crate + its dev-deps).
//!
//! Sprint 0: a single smoke test that every scaffold contract registers and
//! answers `scaffold_version() == 0`. Real lifecycle tests arrive in Sprint 2.

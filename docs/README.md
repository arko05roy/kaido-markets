# docs

- [`adr/`](adr/) — Architecture Decision Records (one file per decision).
- `test-vectors/` — cross-language conformance vectors for the Gaussian /
  curve-fit math, executed by both `kaido-math` (Rust) and `web/lib/curve` (TS).
  See [`test-vectors/README.md`](test-vectors/README.md). Populated from Sprint 1.
- Math derivations mirroring whitepaper Part II land here as the contracts grow.
